import { PaymentMethodDisplayPipe } from './payment-method-display-pipe';

describe('PaymentMethodDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new PaymentMethodDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
